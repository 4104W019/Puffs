


(function ($) {
	var initLayout = function() {

	    //var nownow = new Date();
	    /*
	    var nownow = g_DateArray[0];
		$('#date2').DatePicker({
			flat: true,
			date: nownow,
			current: nownow,
			format: 'Y-m-d',
			calendars: 1,
			mode: 'single',
			onRender: function (date) {
			    var vClass = false;
			    var vDisable = true;
			    for (var i = 0 ; i < g_DateArray.length ; i++) {
			        if (date.valueOf() == g_DateArray[i].valueOf()) {
			            vFlag = 'datepickerSpecial';
			            vDisable = false;
			        }
			    }

			    return {
			        disabled: vDisable,
			        className: vClass,
			    }
			},
			onChange: function(formated, dates) {
			},
			starts: 0
	    
		});

		
		*/
		
		
	};
	
	
	
	EYE.register(initLayout, 'init');
})(jQuery)